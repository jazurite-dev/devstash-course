# Populate Stats & Sidebar

Next, we are going to add the stats as well as the types and the most recent collections in the sidebar.

Be sure you are clearing your context before each feature unless you need it for some reason:

```
/clear
```

## Sidebar Spec

Let's create the spec for this feature. Create `/context/features/stats-sidebar-spec.md` and add the items in the lesson file.

## Load Feature

Let's create our feature in the doc.

```prompt
Add a new current feature. Check @context/features/stats-sidebar-spec.md.
```

Run the feature:

```text
Implement the feature in @context/current-feature.md
```

I want to re-order and re-name the item types:

```text
Order and rename the types within the sidebar - Snippets, Prompts, Notes, Files, Images, Links and make the first letter uppercase
```
